# Resolution mode
resolution-mode=highest
dedupe-peer-dependents=true
save-prefix=""

# Hoist common peer dependencies to the root to avoid multiple copies
public-hoist-pattern[]=react
public-hoist-pattern[]=react-dom
public-hoist-pattern[]=ai
# Hoist tsc to avoid multiple copies
public-hoist-pattern[]=typescript
