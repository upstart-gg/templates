# Datasources Manifests Directory
This directory contains datasources manifests. Each datasource is defined in its own file.
Important: Migrations are auto-generated, no need to create migration files for datasources changes.
